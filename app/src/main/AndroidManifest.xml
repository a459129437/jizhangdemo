<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jizhangdemo">

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="芜湖记账"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BottomBarActivity"></activity>
        <activity android:name=".login.ForgetPasswordActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".login.RegisterActivity" />
        <activity android:name=".login.PasswordLoginActivity" />
        <activity android:name=".login.GraphLoginActivity" />
        <activity android:name=".login.ForgetPasswordActivity2" />
        <activity android:name=".login.ResetPasswordActivity" />
        <activity android:name=".login.LoginMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>